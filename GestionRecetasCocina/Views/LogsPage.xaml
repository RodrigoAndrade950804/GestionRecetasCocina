<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionRecetasCocina.Views.LogsPage"
             Title="Logs">

    <VerticalStackLayout Padding="10" Spacing="10">

        <Button Text="Actualizar Logs" Command="{Binding CargarLogsCommand}" />

        <CollectionView ItemsSource="{Binding Logs}" Margin="0">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5" BackgroundColor="#f5f5f5" CornerRadius="5">
                        <Label Text="{Binding}" FontSize="14" />
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>
